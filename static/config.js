/* Root site config for HoHo Games
 * You can set `window.IDLE_TIMEOUT_SECONDS` here to control the client-side fallback timeout value.
 * Example: window.IDLE_TIMEOUT_SECONDS = 600; // 10 minutes
 */
window.IDLE_TIMEOUT_SECONDS = window.IDLE_TIMEOUT_SECONDS || 3 * 60 * 60; // default 3 hours
